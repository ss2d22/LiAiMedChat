import { Options } from "react-lottie";
import { lottieAnimationData } from "@/assets";
/**
 * animation options for react-lottie for the empty chat container
 * @author Sriram Sundar
 *
 * @type {Options}
 */
export const animationOptions: Options = {
  loop: true,
  autoplay: true,
  rendererSettings: {},
  animationData: lottieAnimationData,
};
